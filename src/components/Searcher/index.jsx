import React from "react";
import { Stack, TextField, IconButton } from "@mui/material";

const Searcher= () => {
  return(
    <Stack>
      <TextField />
      <IconButton />
    </Stack>
  )
}

export default Searcher;
